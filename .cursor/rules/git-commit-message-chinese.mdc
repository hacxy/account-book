---
description: 生成或建议 Git 提交信息时一律使用简体中文（含 IDE 内生成）
alwaysApply: true
---

# Git 提交信息语言

适用范围：**在聊天中撰写提交信息、根据 diff 总结 commit、以及任何「生成提交说明」场景**，均须遵守。

- **提交标题与正文一律使用简体中文**，不得默认输出英文句子。
- 仅当用户**明确说「用英文写提交」**时，才可使用英文。
- 模型易默认用英文写 git message；**必须主动覆盖该习惯**，优先中文。

若使用 Cursor 的「生成 Commit Message」等入口且仍得到英文：在输入框用 `@git-commit-chinese` 引用本规则，或补一句「提交信息用中文」。

## 示例

```text
❌ BAD
fix: region column label in GrowthBoardDrawer

✅ GOOD
fix: 修复增长榜抽屉地区列与筛选项不一致的问题
```

- 类型前缀（如 `fix:`、`feat:`、`docs:`）可保留常见英文约定，**其后说明须为中文**；若团队统一全中文，也可使用 `修复：`、`功能：` 等形式。
