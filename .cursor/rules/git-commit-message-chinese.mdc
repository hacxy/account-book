---
description: 严格要求 Git 提交信息仅使用中文
alwaysApply: true
---

# Git 提交信息强约束规范

当你为本项目生成、建议或改写 Git 提交信息时，必须仅使用简体中文。该规则为强约束，优先级高于默认风格偏好。

## 强制要求

- 提交标题必须是完整中文语句，准确概括变更目的。
- 如需正文说明，正文也必须是完整中文语句。
- 禁止输出英文提交前缀与英文句式，如：`feat:`、`fix:`、`chore:`、`refactor:`、`docs:`、`test:`。
- 禁止输出英文模板化提交信息，如：`update xxx`、`add xxx`、`remove xxx`、`optimize xxx`。
- 禁止中英混写提交信息（专有名词、库名、文件名除外）。

## 输出格式（固定）

- 单行提交信息格式：`<中文动词><中文对象><中文结果/目的>`
- 推荐动词：`修复`、`新增`、`优化`、`重构`、`调整`、`移除`、`完善`

## 合规示例

- `修复账单列表筛选条件未生效的问题`
- `新增首页月度统计卡片并展示本月收支汇总`
- `优化账单详情页渲染逻辑以减少首次加载耗时`
- `重构账本配置读取流程并提升错误提示清晰度`

## 违规示例（禁止输出）

- `fix: resolve filter issue in bill list`
- `feat: add monthly summary card`
- `chore: update dependencies`
- `优化 bill list filter 逻辑`

## 生成前自检（必须全部满足）

- 是否不包含英文提交类型前缀（如 `feat:`、`fix:`）？
- 是否不包含完整英文动词短语（如 `add/update/remove/optimize`）？
- 是否为完整、自然、可读的中文提交语句？
- 是否准确描述“本次变更的目的”，而非仅罗列文件名？
